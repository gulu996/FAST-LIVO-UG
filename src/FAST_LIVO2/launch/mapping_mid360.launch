<launch>
<!-- Launch file for Livox AVIA LiDAR -->
		<arg name="cmd_name" default="" />
		<arg name="rviz" default="true" />

		<!-- Clear a stale value before the main node publishes this run. -->
		<param name="/fast_livo/run_log_directory" value="__PENDING__" />
		<rosparam command="load" file="$(find fast_livo)/config/mid360.yaml" />
		<rosparam command="load" file="$(find fast_livo)/config/gnss_adapter_mid360.yaml" />
		<rosparam command="load" file="$(find fast_livo)/config/rtk_fixed_lag_backend_mid360.yaml" />

	<node pkg="fast_livo" type="fastlivo_mapping" name="laserMapping" output="screen">
		<param name="save_path" type="string" value="$(arg cmd_name)" />
		<param name="use_gnss_fusion_enable" value="true" />
		<rosparam file="$(find fast_livo)/config/camera_pinhole_mid360.yaml" />
	</node>

	<node pkg="fast_livo" type="gnss_adapter_node" name="gnss_adapter" output="screen">
		<param name="use_gnss_fusion_enable" value="true" />
	</node>

	<node pkg="fast_livo" type="rtk_fixed_lag_backend_node" name="rtk_fixed_lag_backend" output="screen">
		<param name="use_gnss_fusion_enable" value="true" />
	</node>
	
	<group if="$(arg rviz)">
		<node launch-prefix="nice" pkg="rviz" type="rviz" name="rviz" args="-d $(find fast_livo)/rviz_cfg/fast_livo2.rviz" />
	</group>
	
	<!--node pkg="image_transport" type="republish" name="republish" args="compressed in:=/left_camera/image raw out:=/left_camera/image" output="screen" respawn="true"/-->

launch-prefix="gdb -ex run --args"
launch-prefix="valgrind --leak-check=full"
</launch> 	
